apiVersion: v1
kind: Service
metadata:
  name: deployguard
  namespace: deployguard
spec:
  type: ClusterIP
  selector:
    app: deployguard
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
